
/**
 * 실행 방법
 * 1. ./gradlew build
 *  - Gradle을 통해 Main.kt 빌드
 * 2. java -cp build/classes/kotlin/main MainKt
 *  - java 명령을 통해 MainKt 실행
 *  - Main.kt 파일을 컴파일 하니 MainKt.class가 되는 것으로 보임 -> 컴파일후 클래스파일명 뒤에 Kt는 기본적으로 붙는 것을 알 수 있음
 */

fun main() {
    println("hello, world")
}

